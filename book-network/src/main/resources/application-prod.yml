spring:
  datasource:
    url: jdbc:postgresql://crossover.proxy.rlwy.net:5777/railway
    username: postgres
    password: Jit0rj5sdgiNoEIYFJvzMigqfuPMysxW
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET}
      expiration: 86400000
      refresh-token:
        expiration: 604800000
  mailing:
    frontend:
      activation-url: ${FRONTEND_URL:http://localhost:4200}/activate-account
  file:
    uploads:
      photos-output-path: ./uploads

server:
  port: ${PORT:8080}